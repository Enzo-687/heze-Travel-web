<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kmbeast.mapper.CategoryMapper">

    <resultMap id="entityMap" type="com.kmbeast.pojo.vo.CategoryVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="level" property="level"/>
        <result column="parent_id" property="parentId"/>
        <result column="has_children" property="hasChildren"/>
    </resultMap>

     <select id="queryPage" resultMap="entityMap">

        SELECT c.id,
        c.name,
        c.level,
        c.parent_id,
        CASE
        WHEN level &lt; 4 AND EXISTS(
        SELECT 1 FROM category ca WHERE ca.parent_id = c.id
        LIMIT 1) THEN true
        ELSE false
        END as has_children
        FROM category c
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE concat('%',#{name},'%')
            </if>
            <if test="level != null">
                AND c.level = #{level}
            </if>
            <if test="parentId != null">
                AND c.parent_id = #{parentId}
            </if>
        </where>
        <if test="current != null and size != null">
            LIMIT #{current},#{size}
        </if>

    </select>

    <select id="queryPageCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM category c
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE concat('%',#{name},'%')
            </if>
            <if test="level != null">
                AND c.level = #{level}
            </if>
            <if test="parentId != null">
                AND c.parent_id = #{parentId}
            </if>
        </where>
    </select>
    <select id="getScenicSpotIds" resultType="java.lang.Integer">

        SELECT ssc.scenic_spot_id AS scenicSpotId FROM scenic_spot_category ssc WHERE ssc.category_id IN
          <foreach collection="categoryIds" item="categoryId" separator="," open="(" close=")" index="index">
              #{categoryId}
          </foreach>

    </select>

</mapper>